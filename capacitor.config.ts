import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.sasasotaro.football3d",
  appName: "FOOTBALL 3D MATCH",
  webDir: "web",
  ios: {
    contentInset: "automatic",
    backgroundColor: "#07131d"
  }
};

export default config;
